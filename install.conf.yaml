- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']
- link:
    ~/.gitconfig: gitconfig
    ~/.emacs.d:
        path: emacs.d
    ~/.config/pass-git-helper:
        path: config/pass-git-helper
    ~/.config/qutebrowser:
        path: config/qutebrowser